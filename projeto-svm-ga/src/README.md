# Para rodar os testes

## Instalação das Dependências

Para executar os testes, você precisa instalar as dependências:

```bash
pip install -r requirements.txt
```

## Executar todos os testes:

```bash
pytest test_ga_search.py -v
```
